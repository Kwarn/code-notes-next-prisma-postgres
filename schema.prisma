datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL") // postgresql://postgres:postgres@localhost:54320/postgres?schema=public
}

model User {
    id        Int      @id @default(autoincrement())
    username  String   @unique
    email     String   @unique
    createdAt DateTime @default(now())
}
